{
  "name": "bmsce-backend",
  "version": "1.0.0",
  "description": "Backend API for BMSCE Connect portal",
  "main": "server.js",
  "scripts": {
    "start": "node --experimental-sqlite server.js",
    "dev": "nodemon --exec \"node --experimental-sqlite\" server.js",
    "seed": "node --experimental-sqlite seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemailer": "^6.9.8"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
